class Movie {
    constructor(tconst, titleType, primaryTitle, runtimeMinutes, genres) {
      this.tconst = tconst;
      this.titleType = titleType;
      this.primaryTitle = primaryTitle;
      this.runtimeMinutes = runtimeMinutes;
      this.genres = genres;
    }
  }
  
  module.exports = Movie;
  